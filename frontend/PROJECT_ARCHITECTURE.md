# 🏗️ MEK-AI 平台项目 - 完整架构与开发总览

## 📋 目录
1. [核心架构与铁律](#-核心架构与铁律-不可违背)
2. [项目结构全览](#-项目结构全览-完整目录树)
3. [当前模块状态](#-当前模块状态-已完成部分)
4. [后续开发计划](#-后续开发计划-阶段化路线图)
5. [技术栈与决策](#-技术栈与关键决策)

---

## ⚙️ 核心架构与铁律 (不可违背)

### 1. 核心模式：AI样式 + 你的逻辑
```
┌─────────────────┐   数据/回调   ┌─────────────────┐
│   ui-pages/     │ ◄─────────── │   modules/      │
│   (AI纯样式层)  │              │   (你的逻辑层)  │
│                 │   事件触发   │                 │
│   • 无状态      │ ───────────► │   • 状态管理    │
│   • 无逻辑      │              │   • 业务逻辑    │
│   • 仅Tailwind  │              │   • API通信     │
└─────────────────┘              └─────────────────┘
```

### 2. 开发工作流 (每个模块必须遵循)
```
1. 规划 → 2. AI样式复刻 → 3. 逻辑层实现 → 4. 桥接集成
   ↓          ↓                ↓              ↓
定义类型  截图→Gemini     types→services   创建Bridge
和接口   → 纯样式组件   → stores→hooks   连接所有部分
```

### 3. 绝对禁止项
- ❌ **禁止** 在 `ui-pages/` 中使用：`useState`, `useEffect`, `useRef`, `useContext`
- ❌ **禁止** 在 `ui-pages/` 中编写：事件处理函数、数据获取逻辑、条件业务逻辑
- ❌ **禁止** 直接修改AI生成的样式组件结构
- ✅ **允许** 在 `ui-pages/` 中使用：条件渲染、列表渲染、从props接收的简单计算

### 4. 数据流原则
- **单向数据流**：`modules/` → (`props`) → `ui-pages/`
- **事件冒泡**：`ui-pages/` → (`callback`) → `modules/`
- **状态管理**：
  - 模块内部状态 → 模块自己的 Store (`modules/*/stores/`)
  - 跨模块共享状态 → 全局 Store (`core/store/`)
  - UI状态(弹窗、视图) → UI专用 Store (`*UIStore.ts`)

---

## 📁 项目结构全览 (完整目录树)

```
src/
├── core/                           # 【核心架构层】- 你的代码
│   ├── layout/                    # 应用布局组件
│   │   └── AppLayout.tsx          # 根布局：侧边栏 + 主内容区 <Outlet />
│   ├── notifications/             # 全局通知系统
│   │   ├── hooks/
│   │   │   └── useNotifications.ts
│   │   └── stores/
│   │       └── notificationsStore.ts
│   ├── router/                    # 路由配置中心
│   │   └── index.tsx              # React Router v6 路由定义
│   └── store/                     # 【全局状态】跨模块共享
│       └── userStore.ts           # 用户全局数据（已招聘员工列表）
│
├── modules/                       # 【业务模块层】- 按功能划分
│   ├── digital-employee/          # 数字员工模块（已实现）
│   │   ├── components/            # 桥接组件
│   │   │   ├── CreateDigitalEmployeeBridge.tsx  # 创建数字员工桥接
│   │   │   └── EditDigitalEmployeeBridge.tsx    # 编辑数字员工桥接
│   │   ├── logic/                 # 业务逻辑层
│   │   │   ├── hooks/
│   │   │   │   ├── useDigitalEmployee.ts        # 数字员工列表逻辑
│   │   │   │   ├── useDigitalEmployeeCreation.ts # 创建逻辑
│   │   │   │   └── useDigitalEmployeeEditor.ts  # 编辑器逻辑
│   │   │   ├── services/
│   │   │   │   └── mockData.ts                  # Mock数据与API
│   │   │   └── stores/
│   │   │       ├── digitalEmployeeStore.ts      # 数字员工状态
│   │   │       └── digitalEmployeeEditorStore.ts # 编辑器状态
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── DigitalEmployeeBridge.tsx            # 主桥接组件
│   │   └── routes.tsx
│   │
│   ├── knowledge-base/            # 知识库模块（已实现）
│   │   ├── logic/
│   │   │   ├── hooks/
│   │   │   │   ├── useKnowledgeBase.ts          # 知识库列表逻辑
│   │   │   │   ├── useKnowledgeBaseCreation.ts  # 创建逻辑
│   │   │   │   └── useKnowledgeBaseFile.ts      # 文件管理逻辑
│   │   │   ├── services/
│   │   │   │   ├── knowledgeBaseService.ts      # 服务接口
│   │   │   │   └── mockData.ts
│   │   │   ├── stores/
│   │   │   │   ├── knowledgeBaseStore.ts        # 知识库状态
│   │   │   │   └── knowledgeBaseUIStore.ts      # UI状态
│   │   │   └── utils/
│   │   │       └── index.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── KnowledgeBaseBridge.tsx              # 桥接组件
│   │   └── routes.tsx
│   │
│   ├── marketplace/               # AI员工广场模块（已实现）
│   │   ├── logic/
│   │   │   ├── hooks/
│   │   │   │   ├── useMarketplace.ts            # 市场数据逻辑
│   │   │   │   ├── useMarketplaceInteraction.ts # 交互逻辑
│   │   │   │   └── useChatLogic.ts              # 聊天业务逻辑
│   │   │   ├── services/
│   │   │   │   └── mockData.ts
│   │   │   └── stores/
│   │   │       ├── marketplaceStore.ts          # 市场数据状态
│   │   │       ├── marketplaceUIStore.ts        # UI状态
│   │   │       └── chatStore.ts                 # 聊天状态
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── ChatBridge.tsx                       # 聊天页面桥接
│   │   └── EnhancedMarketplaceBridge.tsx        # 市场主桥接
│   │
│   └── under-development/         # 通用占位模块
│       └── UnderDevelopmentBridge.tsx           # "开发中"统一提示
│
├── shared/                       # 【共享资源层】- 跨模块复用
│   ├── components/               # 共享UI组件
│   │   ├── AppSidebar.tsx        # 左侧导航栏（受控组件）
│   │   └── NotificationContainer.tsx # 全局通知弹窗
│   ├── hooks/                    # 共享逻辑Hooks
│   │   ├── useNotification.ts    # 通知Hook
│   │   └── useSidebarLogic.ts    # 侧边栏业务逻辑
│   ├── types/                    # 共享类型定义
│   │   ├── digitalEmployee.ts    # 数字员工类型
│   │   ├── employee.ts           # 员工基础类型
│   │   ├── knowledge.ts          # 知识库类型
│   │   └── typeAdapters.ts       # 类型适配器
│   └── utils/                    # 工具函数
│       └── avatar.ts             # 头像生成工具
│
├── ui-pages/                     # 【AI样式层】- Gemini生成的纯样式组件
│   ├── digital-employee/         # 数字员工样式组件
│   │   ├── components/
│   │   │   ├── CreateDigitalEmployeeModal.tsx   # 创建弹窗
│   │   │   ├── DigitalEmployeesPage.tsx         # 主页面
│   │   │   ├── EditDigitalEmployee.tsx          # 编辑页面
│   │   │   └── EmployeeCard.tsx                 # 员工卡片
│   │   └── types.ts
│   │
│   ├── knowledge-base/           # 知识库样式组件
│   │   ├── components/
│   │   │   ├── AddFile.tsx                      # 添加文件
│   │   │   ├── BuildKnowledgeBase.tsx           # 创建知识库
│   │   │   └── KnowledgeBase.tsx                # 知识库列表
│   │   └── types.ts
│   │
│   └── marketplace/              # AI员工广场样式组件
│       ├── components/
│       │   ├── CategoryFilter.tsx               # 分类筛选
│       │   ├── ChatInterface.tsx                # 聊天界面
│       │   ├── EmployeeCard.tsx                 # 员工卡片
│       │   ├── MarketplacePage.tsx              # 市场主页面
│       │   └── RecruitmentModal.tsx             # 招聘弹窗
│       └── types.ts
│
├── styles/                       # 全局样式
│   └── globals.css               # Tailwind/自定义全局样式
│
├── App.tsx                       # 应用根组件
└── main.tsx                      # 应用入口
```

### 关键文件说明
- **`DigitalEmployeeBridge.tsx`**：数字员工模块入口，整合列表、创建、编辑功能
- **`EnhancedMarketplaceBridge.tsx`**：市场模块入口，连接AI组件与业务逻辑
- **`KnowledgeBaseBridge.tsx`**：知识库模块入口，管理文档和向量化
- **`marketplaceStore.ts`**：市场数据状态管理，包含招聘功能与userStore同步
- **`digitalEmployeeStore.ts`**：数字员工状态管理，支持创建和编辑
- **`knowledgeBaseStore.ts`**：知识库状态管理，支持文档上传
- **`chatStore.ts`**：聊天数据当前存储在内存中，刷新页面会丢失
- **`userStore.ts`**：全局用户状态，管理已招聘员工列表
- **`notificationsStore.ts`**：全局通知系统，支持success/warning/error/info四种类型
- **`typeAdapters.ts`**：类型适配器，处理不同层之间的类型转换

---

## 📊 当前模块状态 (已完成部分)

### ✅ AI员工广场模块 (完成度 100%)
- **功能**：员工展示、分类筛选、搜索、招聘、聊天、弹窗
- **状态**：完全遵循架构，Mock数据可运行
- **核心文件**：
  - [EnhancedMarketplaceBridge.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/marketplace/EnhancedMarketplaceBridge.tsx)
  - [marketplaceStore.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/marketplace/logic/stores/marketplaceStore.ts)
  - [chatStore.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/marketplace/logic/stores/chatStore.ts)

### ✅ 数字员工模块 (完成度 100%)
- **功能**：展示已创建/招聘员工列表、Tab切换、创建数字员工、编辑数字员工、跳转到聊天
- **状态**：功能完整，支持人设/技能/知识库配置
- **核心文件**：
  - [DigitalEmployeeBridge.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/digital-employee/DigitalEmployeeBridge.tsx)
  - [useDigitalEmployee.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/digital-employee/logic/hooks/useDigitalEmployee.ts)
  - [useDigitalEmployeeEditor.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/digital-employee/logic/hooks/useDigitalEmployeeEditor.ts)
  - [digitalEmployeeStore.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/digital-employee/logic/stores/digitalEmployeeStore.ts)
  - [EditDigitalEmployee.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/ui-pages/digital-employee/components/EditDigitalEmployee.tsx)

### ✅ 知识库模块 (完成度 100%)
- **功能**：知识库列表、创建知识库、文档上传、与数字员工关联
- **状态**：功能完整，支持文件管理
- **核心文件**：
  - [KnowledgeBaseBridge.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/knowledge-base/KnowledgeBaseBridge.tsx)
  - [useKnowledgeBase.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/knowledge-base/logic/hooks/useKnowledgeBase.ts)
  - [knowledgeBaseStore.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/modules/knowledge-base/logic/stores/knowledgeBaseStore.ts)

### ✅ 核心架构设施 (完成度 100%)
- **路由与布局**：`AppLayout` + 侧边栏导航，支持多级菜单
- **全局状态**：`userStore` 基础框架，支持已招聘员工管理
- **通知系统**：全局Toast提示，支持多种类型
- **开发占位**：`UnderDevelopmentBridge` 统一处理未开发页面
- **类型系统**：完整的类型定义和适配器
- **核心文件**：
  - [AppLayout.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/core/layout/AppLayout.tsx)
  - [router/index.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/core/router/index.tsx)
  - [userStore.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/core/store/userStore.ts)
  - [typeAdapters.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/shared/types/typeAdapters.ts)

### ✅ 共享组件 (完成度 100%)
- **侧边栏**：已迁移并改为受控组件，支持多级导航、徽章、展开/折叠
- **通知容器**：已实现，支持自动关闭和手动关闭
- **共享类型**：统一的Employee类型定义，包含normalizeEmployee函数
- **类型适配器**：处理AI组件层、业务逻辑层和页面层之间的类型转换
- **核心文件**：
  - [AppSidebar.tsx](file:///d:/Project/MEK-AI/MEK-AI-V2/src/shared/components/AppSidebar.tsx)
  - [useSidebarLogic.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/shared/hooks/useSidebarLogic.ts)
  - [employee.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/shared/types/employee.ts)
  - [typeAdapters.ts](file:///d:/Project/MEK-AI/MEK-AI-V2/src/shared/types/typeAdapters.ts)

---

## 🗺️ 后续开发计划 (阶段化路线图)

### 第一阶段：企业级功能 (预计：3周)
**目标**：实现企业管理和权限控制

| 模块 | 核心功能 | 技术要点 |
|------|----------|----------|
| **1. 我的企业**<br>(`my-business/`) | • 企业信息管理<br>• 企业成员管理<br>• 企业设置 | • 表单验证<br>• 成员权限控制 |
| **2. 角色管理**<br>(`role-management/`) | • 角色创建/编辑<br>• 权限配置<br>• 角色分配 | • 权限树组件<br>• 复杂表单状态 |
| **3. 账号管理**<br>(`account-management/`) | • 账号列表<br>• 账号状态管理<br>• 账号操作日志 | • 表格组件<br>• 分页/筛选 |

### 第二阶段：集成与扩展 (预计：2周)
**目标**：实现第三方集成和群组功能

| 模块 | 核心功能 | 技术要点 |
|------|----------|----------|
| **1. 端口接入**<br>(`wecom/`, `wechat/`) | • 企业微信接入<br>• 个人微信接入<br>• 消息同步 | • OAuth认证<br>• WebSocket实时通信 |
| **2. AI数字员工群**<br>(`groups/`) | • 群组创建/管理<br>• 群组成员分配<br>• 群组对话 | • 复杂状态管理<br>• 多人聊天逻辑 |

### 第三阶段：高级功能 (预计：2周)
**目标**：实现高级AI功能和数据分析

| 模块 | 核心功能 | 技术要点 |
|------|----------|----------|
| **1. 标签库管理**<br>(`tag-library/`) | • 标签创建/编辑/删除<br>• 标签分类<br>• 标签搜索 | • 简单CRUD操作<br>• 拖拽排序 |
| **2. 数据分析**<br>(`analytics/`) | • 使用统计<br>• 对话分析<br>• 性能报表 | • 图表组件<br>• 数据可视化 |

---

## 🛠️ 技术栈与关键决策

### 核心技术栈
```json
{
  "dependencies": {
    "react": "^19.2.4",           // UI框架
    "react-dom": "^19.2.4",       // DOM渲染
    "react-router-dom": "^7.13.0", // 路由管理
    "zustand": "^5.0.10",          // 状态管理
    "axios": "^1.13.3",            // HTTP客户端
    "lucide-react": "^0.563.0"    // 图标库
  },
  "devDependencies": {
    "typescript": "^5.9.3",       // 类型系统
    "vite": "^7.3.1",              // 构建工具
    "@vitejs/plugin-react": "^5.1.2"
  }
}
```

### 关键架构决策

#### 1. 状态管理选择：Zustand
**理由**：
- ✅ 轻量级，无样板代码
- ✅ 支持TypeScript类型推断
- ✅ 支持中间件（persist、devtools）
- ✅ 简单的API，学习成本低

#### 2. 路由选择：React Router v7
**理由**：
- ✅ 嵌套路由支持（配合 `<Outlet />`）
- ✅ 声明式路由配置
- ✅ 支持懒加载和代码分割
- ✅ 与React生态深度集成

#### 3. 样式方案：Tailwind CSS
**理由**：
- ✅ 原子化CSS，快速开发
- ✅ 响应式设计开箱即用
- ✅ 可定制主题
- ✅ 生产环境体积小

#### 4. 构建工具：Vite
**理由**：
- ✅ 极速冷启动
- ✅ 即时热更新（HMR）
- ✅ 原生ES模块支持
- ✅ 优化的生产构建

### 数据流设计原则

```
┌─────────────────────────────────────────────────────────────┐
│                        数据流设计                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 单向数据流                                               │
│     Store → Props → UI Component                            │
│                                                             │
│  2. 事件冒泡                                                 │
│     UI Component → Callback → Store/Service                 │
│                                                             │
│  3. 状态分层                                                 │
│     - 全局状态: core/store/ (userStore)                     │
│     - 模块状态: modules/*/stores/ (marketplaceStore)        │
│     - UI状态: modules/*/stores/*UIStore.ts                  │
│                                                             │
│  4. 数据持久化                                               │
│     - 暂存: 内存 (chatStore)                                │
│     - 持久: localStorage (userStore + zustand/persist)       │
│     - 同步: API (待实现)                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发规范

#### 文件命名规范
- 组件文件：`PascalCase.tsx` (如 `MarketplacePage.tsx`)
- Hook文件：`use*.ts` (如 `useMarketplace.ts`)
- Store文件：`*Store.ts` (如 `marketplaceStore.ts`)
- Service文件：`*Service.ts` (如 `marketplaceService.ts`)
- 类型文件：`index.ts` 或 `types.ts`

#### 目录组织规范
```
module-name/
├── logic/              # 业务逻辑层
│   ├── hooks/          # 自定义Hooks
│   ├── services/       # 数据服务
│   ├── stores/         # 状态管理
│   └── utils/          # 工具函数
├── types/              # 类型定义
└── *Bridge.tsx         # 桥接组件（模块入口）
```

#### 代码风格规范
- 使用TypeScript严格模式
- 组件使用函数式组件 + Hooks
- 避免使用 `any` 类型，使用 `unknown` 或具体类型
- 使用 `const` 优于 `let`
- 使用箭头函数保持 `this` 上下文

---

## 📈 项目统计

### 代码规模
- **总文件数**：约60个文件
- **核心模块**：4个（marketplace, digital-employee, knowledge-base, under-development）
- **共享组件**：2个（AppSidebar, NotificationContainer）
- **路由数量**：17个（其中12个指向开发中页面）

### 完成度统计
- **核心架构**：100% ✅
- **AI员工广场**：100% ✅
- **数字员工**：100% ✅
- **知识库**：100% ✅
- **标签库**：0% ⏳
- **企业管理**：0% ⏳
- **端口接入**：0% ⏳

### 技术债务
1. `ChatInterface.tsx` 中的滚动逻辑需要移至 `useChatLogic`
2. `frontend/` 目录需要删除（旧版代码）
3. 缺少API客户端配置（需要接入真实后端）
4. 缺少错误边界组件
5. 缺少加载状态组件
6. 需要添加单元测试

---

## 📝 总结

MEK-AI 平台采用 **AI样式层 + 业务逻辑层** 的分离架构，严格遵循单向数据流原则。项目已完成核心架构搭建，实现了AI员工广场、数字员工和知识库三大核心模块，为后续开发奠定了坚实基础。

### 核心优势
- ✅ 清晰的分层架构，易于维护和扩展
- ✅ 完整的类型系统，减少运行时错误
- ✅ 统一的状态管理方案，数据流清晰
- ✅ 模块化设计，便于团队协作
- ✅ 类型适配器解决跨层类型兼容问题

### 下一步行动
1. 实现企业管理和权限控制功能
2. 接入第三方平台（企业微信、个人微信）
3. 实现AI数字员工群组功能
4. 接入真实后端API
5. 完善测试覆盖
